import { Component } from "preact";
import { MoveableProps, PreactMoveableInterface } from "preact-moveable";
export default class InnerMoveable extends Component<MoveableProps> {
    state: MoveableProps;
    preactMoveable: PreactMoveableInterface;
    constructor(props: MoveableProps);
    render(): import("preact").VNode<any>;
}
